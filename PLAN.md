# Plan
I want to refactor this dashboard template into using this stack:
- Typescript with Bun Runtime
- Nuxt 4 with serverside rendering for each page
- Support dark and light mode
- Use Tailwind CSS for styling
- Each time you make a feature change, add commit with a message that describes the change but dont push

Use `playwright mcp` to test all of the ui component feature